class Executing:
    def __init__(self):
        (
            self.SCORE_TEXT,
            self.HIGH_SCORE_TEXT,
            self.LEVEL_TEXT,
            self.READY_TEXT,
            self.PAUSE_TEXT,
            self.GAME_OVER_TEXT,
        ) = (0, 1, 2, 3, 4, 5)
